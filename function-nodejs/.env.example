# Claude API
ANTHROPIC_API_KEY=sk-ant-...

# PostgreSQL MCP HTTP Server
POSTGRES_MCP_URL=https://your-mcp-server.execute-api.us-west-2.amazonaws.com/prod
POSTGRES_MCP_TOKEN=secret-token-for-mcp-auth

# Database Credentials (for MCP server, not main Lambda)
DB_USER=your_db_username
DB_PASSWORD=your_db_password
DB_HOST=your_db_host
DB_PORT=5432
DB_NAME=your_database_name

# AWS Region (optional override)
AWS_REGION=us-west-2
